# Copy sang apps/admin-web/.env truoc khi chay dev.
# URL cua NestJS API (apps/api). Chi doc o server component, khong lo ra client.
API_URL="http://localhost:4000"
# Login/session van do api-legacy (Hono) so huu - Nest moi chi verify JWT.
# Bo bien nay khi auth da migrate sang apps/api.
LEGACY_API_URL="http://localhost:3001"
